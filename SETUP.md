# 行前夹 TripClip · Supabase 自建项目手册（v0.6）

> 目的：让 **Mac 记录、手机查看** 的数据自动互通。  
> 原理：数据存到 Supabase 云数据库，任何设备登录同一账号即可读写同一份数据。  
> 耗时：一次性配置约 15 分钟，之后免维护。

---

## ⚡ 开箱即用版（大多数用户只看这里）

**当前线上版本已内置云端配置**，新用户打开页面就能直接注册使用，**不需要做本手册的任何步骤**。

只有以下情况才需要自建 Supabase 项目：
- 想用**自己的数据库**（数据完全自己掌控）
- 内置项目被暂停 / 网络不可达 / 想换区域
- 想自托管一份给团队或朋友用

自建时在应用设置面板（☁️ 云同步设置）改填自己的 URL 和 key 即可覆盖内置配置；点「恢复内置配置」可随时回到默认。

---

## 第 1 步：创建 Supabase 项目（免费）

1. 打开 👉 https://supabase.com  → 点 **Start your project** 注册登录（可用 GitHub 账号）
2. 登录后点 **New project**：
   - **Name**：填 `tripclip`
   - **Database Password**：设置一个密码（自己记好，以后恢复备份用）
   - **Region**：选 **Southeast Asia (Singapore)** 或 **Northeast Asia (Tokyo)** —— 离国内近，访问更快
3. 点 **Create new project**，等 1-2 分钟创建完成

## 第 2 步：建数据库表（最关键，别跳过）

1. 左侧菜单点 **SQL Editor** → **New query**
2. 把仓库里的 `supabase-schema.sql` 全文复制粘贴进去
3. 点 **Run**（底部显示 Success 即成功）

这一步建好了数据表和**行级安全策略**——保证每个账号只能看到自己的数据，这是安全的核心。

## 第 3 步：拿到项目地址和密钥

1. 左侧菜单点 **Settings**（齿轮）→ **API**
2. 复制两个值：
   - **Project URL**（形如 `https://xxxx.supabase.co`）
   - **Project API keys → anon public**（新项目是 `sb_publishable_...` 开头的短 key；老项目是 `eyJhbGci...` 长 JWT，两者都有效）
3. 这两个值本来就是公开给前端的，安全靠第 2 步的行级权限，不用保密

## 第 4 步：修改「确认邮件链接」地址（关键，不改注册不成功）

Supabase 新项目默认的 Site URL 是 `http://localhost:3000`，不改的话注册邮件里的确认链接指向 localhost，点不开，账号永远确认不了。

1. 左侧菜单点 **Authentication** → **URL Configuration**
2. 把 **Site URL** 改成你应用的线上地址（**不带**末尾多余字符，注意含 `/tripclip/` 子路径）：
   - 例如：`https://你的用户名.github.io/tripclip/`
3. 在 **Redirect URLs** 里点 **Add redirect URL**，添加：
   - `https://你的用户名.github.io/tripclip/**`
4. 点 **Save**

## 第 5 步：填进应用（覆盖内置配置）

1. 打开行前夹（必须是线上地址 `https://你的用户名.github.io/tripclip/`，不是本地文件）
2. 点 **「☁️ 云同步设置」** 展开 → 输入框应已预填内置默认值，把 **Project URL** 和 **anon key** 换成你自建项目的值
3. 点 **保存配置**，看到「✓ 配置已保存」即成功（想回退内置：点「恢复内置配置」）

## 第 6 步：注册账号

1. 点页面右上角 **「👤 未登录」** → 选 **没有账号？注册** → 填邮箱和密码（至少 6 位）→ 注册
2. **去邮箱点确认链接**（链接应以 `https://你的用户名.github.io/tripclip/` 开头；若已改过 Site URL 但之前发的邮件还是 localhost，请重新注册一次或让 Supabase 重发）
3. 点完确认链接，回到页面右上角「👤 未登录」→ **登录**，页面顶部出现账户条（绿色同步状态）

## 第 7 步：迁移本地数据到云端（仅首次）

- 如果本机之前存过收藏，登录后会弹窗询问：**「是否上传到云端，实现 Mac 与手机数据互通？」** → 点 **确定**
- 上传完成后，顶部显示 **「✓ 已上传 N 条」**

## 第 8 步：手机上看

1. 手机浏览器打开你的站点（如 `https://你的用户名.github.io/tripclip/`）
2. 右上角「👤 未登录」→ **登录**（同一账号）
3. 数据自动从云端加载，Mac 上记的每条都在

---

## 日常使用

| 场景            | 表现                                         |
| ------------- | ------------------------------------------ |
| Mac 上添加/编辑/删除 | 自动同步到云端，手机刷新即见                             |
| 手机上添加/编辑/删除   | 自动同步回云端，Mac 刷新即见                           |
| 断网 / 无信号      | 能查看上次同步的快照；改动会提示「待同步」，联网后点 **「↻ 重新同步」** 重试 |
| 换电脑 / 清浏览器    | 登录同一账号，数据自动回来                              |

## 常见问题

**Q：anon key 公开了安全吗？**  
安全。key 只是"入口凭证"，真正拦数据的是数据库的行级安全策略（RLS）——未登录用户连表都访问不了，登录用户也只能读写自己的行。

**Q：注册邮件里的确认链接打不开 / 是 localhost？**  
Supabase 新项目默认 Site URL 是 `http://localhost:3000`。去 **Authentication → URL Configuration** 把 **Site URL** 改成线上地址（如 `https://你的用户名.github.io/tripclip/`），并添加 Redirect URL `https://你的用户名.github.io/tripclip/**`，保存后重新注册即可。已发出的旧邮件链接不会变，需重新注册。

**Q：国内访问慢/打不开怎么办？**  
项目区域选的新加坡/东京一般可用。如果完全不可用，备选方案是换 LeanCloud（国内版），需要另建项目，结构类似。

**Q：忘记密码？**  
打开 Supabase 项目 → **Authentication → Users** → 找到你的用户 → **Send password recovery**，会发一封重置邮件。

**Q：数据安全吗？会丢吗？**  
数据同时存在云端（Supabase）和本机缓存，双保险。仍然建议定期用「导出 Markdown / CSV」做离线备份。

**Q：我不用账号，只想本地用行吗？**  
行。不登录就完全是之前的纯本地模式，功能不受影响。账号只在你需要多设备同步时才需要。
